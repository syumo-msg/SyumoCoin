module.export = {
    networks: {
        // Define your network configurations here (e.g., development, ropsten, mainnet, etc.)
    },
    compilers: {
        solc: {
            version: "0.8.7", // Use the desired Solidity compiler version

        },
    },
};